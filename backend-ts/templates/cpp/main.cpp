/**
 * Student Grade Manager
 *
 * A modern C++17 application for managing student grades
 * with colored terminal output and interactive menus.
 */

#include "src/app.h"

int main() {
    GradeManager manager;
    manager.run();
    return 0;
}
